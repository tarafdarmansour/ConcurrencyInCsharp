@model ParallelProcessingDemo.Models.ParallelDemoViewModel

@{
    ViewData["Title"] = Model.Title;
}

<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1>@Model.Title</h1>
                    <p class="lead">@Model.Description</p>
                </div>
                <a href="@Url.Action("Index")" class="btn btn-outline-primary">
                    <i class="fas fa-arrow-left"></i> بازگشت به خانه
                </a>
            </div>
        </div>
    </div>

    <div class="row mb-4">
        <div class="col-md-6">
            <div class="card bg-light">
                <div class="card-body text-center">
                    <h5 class="card-title">روش پردازش</h5>
                    <p class="card-text">@Model.ParallelMethod</p>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card bg-info text-white">
                <div class="card-body text-center">
                    <h5 class="card-title">زمان اجرا</h5>
                    <p class="card-text">@Model.ExecutionTime ms</p>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">۱۰ مشتری با بیشترین خرید</h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>نام مشتری</th>
                                    <th>منطقه</th>
                                    <th>مجموع خرید</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.PlinqResults != null)
                                {
                                    foreach (var customer in Model.PlinqResults)
                                    {
                                        <tr>
                                            <td>@customer.Name</td>
                                            <td>@customer.Region</td>
                                            <td>@customer.TotalPurchases.ToString("C0")</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">آمار بر اساس منطقه</h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>منطقه</th>
                                    <th>میانگین خرید</th>
                                    <th>تعداد مشتریان</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.RegionStats != null)
                                {
                                    foreach (var region in Model.RegionStats)
                                    {
                                        <tr>
                                            <td>@region.Region</td>
                                            <td>@region.AveragePurchase.ToString("C0")</td>
                                            <td>@region.CustomerCount</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-light">
                    <h5 class="mb-0">کد استفاده شده</h5>
                </div>
                <div class="card-body">
                    <pre><code class="language-csharp">// PLINQ Example
var highValueCustomers = customers
    .AsParallel()
    .Where(c => c.TotalPurchases > 5000)
    .OrderByDescending(c => c.TotalPurchases)
    .Take(10)
    .ToList();

var averageByRegion = customers
    .AsParallel()
    .GroupBy(c => c.Region)
    .Select(g => new
    {
        Region = g.Key,
        AveragePurchase = g.Average(c => c.TotalPurchases),
        CustomerCount = g.Count()
    })
    .OrderByDescending(r => r.AveragePurchase)
    .ToList();</code></pre>
                </div>
            </div>
        </div>
    </div>
</div>
